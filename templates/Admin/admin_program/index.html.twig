{% extends 'base.html.twig' %}

{% block title %}Program index{% endblock %}

{% block body %}
    <h1>Program index</h1>
<div class="d-flex justify-content-center">
    {{ form(form) }}
    <a class="btn btn-info" href="{{ path('app_admin_program_index') }}" >Reset</a>
</div>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Poster</th>
                <th>Synopsis</th>
                <th>Country</th>
                <th>Year</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for program in programs %}
            <tr>
                <td>{{ program.id }}</td>
                <td>{{ program.title }}</td>
                <td>{{ program.poster }}</td>
                <td>{{ program.synopsis }}</td>
                <td>{{ program.country }}</td>
                <td>{{ program.year }}</td>
                <td>
                    <a href="{{ path('app_admin_program_show', {'id': program.id}) }}">show</a>
                    <a href="{{ path('app_admin_program_edit', {'id': program.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<div class="d-flex justify-content-center">
    {{ knp_pagination_render(programs) }}
</div>
    <a href="{{ path('app_admin_program_new') }}">Create new</a>
{% endblock %}
